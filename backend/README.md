# Backend

Java + Spring Boot 기반 계산 엔진 및 API 서버입니다.

## Run

```bash
./gradlew bootRun
```

Windows:

```bash
gradlew.bat bootRun
```

## Main API

```http
POST /api/simulations/full
GET /api/risk-events
POST /api/simulations/risk-event
```
